module esm::Plugin

import esm::Parser;
import esm::CST2AST;
import esm::Check;
import esm::CodeGen;
import IO;

// registerLanguage(...) and folder-walking helpers over examples/ go here
// once the language server contribution is wired up.

void main() {
  println("esm plugin stub");
}
